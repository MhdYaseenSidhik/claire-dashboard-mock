// Linting is deferred to a later ticket. TypeScript strict mode (npm run typecheck)
// and the test suite (npm test) are the quality gates in this baseline.
export default [{ ignores: ['dist', 'node_modules'] }];
